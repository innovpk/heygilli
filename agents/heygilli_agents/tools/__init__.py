"""Strands `@tool` functions shared by the HeyGilli agents.

Every tool is a plain Python function that can also be called directly from
code (the decorator keeps it callable), so the deterministic pipeline and the
agents use the same implementations.
"""
