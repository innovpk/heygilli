# secrets
.env
*.pem
# python
agents/.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# flutter / dart
app/.dart_tool/
app/.packages
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/local.properties
app/android/key.properties
app/android/**/*.jks
app/.idea/
*.iml
# design canvas build output (regenerated by design/generate.py + seed step)
design/heygilli-mockups.html
# vercel, gone but the local link metadata lingers
.vercel
# os
.DS_Store
public/
build/site/
.wrangler/

# A 2.8MB ad clip that lives in this folder but is not part of the site.
# Nothing references it, and app/web/ is copied wholesale by deploy_web.sh.
app/web/heygilli_ad_10s.mp4
